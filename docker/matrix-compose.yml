version: "3"

services:
  synapse:
    image: matrixdotorg/synapse:latest
    container_name: zemi_matrix
    restart: always
    volumes:
      - ./matrix_data:/data
    environment:
      - SYNAPSE_SERVER_NAME=localhost
      - SYNAPSE_REPORT_STATS=no
    ports:
      - "8008:8008"
